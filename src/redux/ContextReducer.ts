import {
  SET_CONTEXT_OF_RESOURCE
} from "./ContextAction";

const INIT_STATE = {
  context: "",
};

// const ContextReducer = (state = INIT_STATE, action) => {
//   switch (action.type) {
//     case SET_CONTEXT_OF_RESOURCE:
//       return {
//         ...state,
//         context: action.payload
//       };
//     default:
//       return state;
//   }
// };

// export default ContextReducer;
